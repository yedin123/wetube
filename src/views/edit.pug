extends base.pug

block content
    h4 Change Title of Video
    form(method="POST")
        input(name="newTitle",placeholder="New Title", value=video.title, required)
        input(value="Save", type="submit")
